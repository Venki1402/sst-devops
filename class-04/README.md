# Notes

- linux architecture -- hardware < kernal < shell < user programs
- #! - used for shebang, # used for comment
- pass - `user@123!` -- for root
- `sudo su` -> go to super user
- `su - 'devs'` -> switch user
- `set -e` -> Immediately exit when there is an error
